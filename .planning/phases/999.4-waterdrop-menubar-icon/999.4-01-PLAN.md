---
phase: 999.4-waterdrop-menubar-icon
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - PomoHydro/PomoHydro/Views/MenuBarIconView.swift
  - PomoHydro/PomoHydro/PomoHydroApp.swift
autonomous: false
requirements: []

must_haves:
  truths:
    - "Menu bar shows a composite clock+waterdrop icon instead of a single SF Symbol"
    - "Clock portion visually changes between idle, working, break, and paused states"
    - "Waterdrop is always visible regardless of app state"
  artifacts:
    - path: "PomoHydro/PomoHydro/Views/MenuBarIconView.swift"
      provides: "Custom-drawn composite menu bar icon"
      min_lines: 40
  key_links:
    - from: "PomoHydro/PomoHydro/PomoHydroApp.swift"
      to: "MenuBarIconView"
      via: "MenuBarExtra label closure"
      pattern: "MenuBarIconView"
---

<objective>
Replace the current SF Symbol menu bar icon with a custom-drawn composite icon: a clock with a half-overlapping waterdrop. The clock portion reflects Pomodoro state (idle, working, break, paused) while the waterdrop remains constant as the PomoHydro brand mark.

Purpose: Give PomoHydro a unique, recognizable identity in the menu bar that communicates both its timer and hydration features at a glance.
Output: Custom SwiftUI icon view wired into the MenuBarExtra label.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/999.4-waterdrop-menubar-icon/999.4-CONTEXT.md
@.planning/ROADMAP.md
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From PomoHydro/PomoHydro/Services/PomodoroService.swift:

```swift
enum PomodoroState: String, Codable {
    case idle
    case working
    case shortBreak
    case longBreak
    case autoStartCountdown
}

// Current menuBarIcon computed property (to be consumed by new view):
var menuBarIcon: String {
    if allPaused { return "pause.circle" }
    switch state {
    case .idle, .autoStartCountdown: return "timer"
    case .working: return "timer.circle.fill"
    case .shortBreak, .longBreak: return "cup.and.saucer"
    }
}
```

From PomoHydro/PomoHydro/PomoHydroApp.swift:

```swift
// Current menu bar label (line 30):
} label: {
    Image(systemName: pomodoroService.menuBarIcon)
}
```

</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create custom MenuBarIconView with state-driven clock + constant waterdrop</name>
  <files>PomoHydro/PomoHydro/Views/MenuBarIconView.swift</files>
  <read_first>
    - PomoHydro/PomoHydro/Services/PomodoroService.swift (PomodoroState enum and menuBarIcon property)
    - PomoHydro/PomoHydro/PomoHydroApp.swift (current label usage)
  </read_first>
  <action>
Create a new SwiftUI View `MenuBarIconView` that draws a composite icon using SwiftUI `Canvas` or `Path`:

1. **Clock element (left):** A circle with clock hands or a timer indicator. Must visually differentiate 4+1 states:
   - `idle` / `autoStartCountdown`: Simple clock outline (unfilled)
   - `working`: Filled clock or clock with a progress-like fill
   - `shortBreak` / `longBreak`: Clock outline with a distinct break indicator (e.g., pause bars inside, or a softer/dashed outline)
   - `allPaused`: Clock with pause bars overlay

2. **Waterdrop element (right, half-overlapping the clock):** A teardrop/waterdrop shape, always filled, always present. Positioned so roughly 20-30% of its width overlaps the clock circle's right edge.

3. **Sizing:** Menu bar icons are typically 18x18pt. The composite should fit within roughly 22x18pt (slightly wider to accommodate both elements). Use `.renderingMode(.template)` behavior — the icon should respect the system's menu bar tint (light/dark mode). Draw using the `primary` foreground style so macOS handles light/dark automatically.

4. **Input:** Accept `PomodoroState` and `Bool` (allPaused) as parameters. Do NOT use @Environment — the parent will pass these values explicitly so the view stays pure and testable.

```swift
struct MenuBarIconView: View {
    let state: PomodoroState
    let allPaused: Bool
    // ... Canvas or ZStack with Path shapes
}
```

  </action>
  <verify>
    <automated>test -f PomoHydro/PomoHydro/Views/MenuBarIconView.swift && grep -q "struct MenuBarIconView" PomoHydro/PomoHydro/Views/MenuBarIconView.swift && grep -q "PomodoroState" PomoHydro/PomoHydro/Views/MenuBarIconView.swift && grep -q "allPaused" PomoHydro/PomoHydro/Views/MenuBarIconView.swift && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - MenuBarIconView.swift exists in PomoHydro/PomoHydro/Views/
    - Contains `struct MenuBarIconView: View`
    - Accepts `state: PomodoroState` and `allPaused: Bool` parameters
    - Contains drawing code for both clock and waterdrop shapes (grep for "Path" or "Canvas" or "Circle" or "Shape")
    - Handles all PomodoroState cases (grep for "idle", "working", "shortBreak", "longBreak")
    - Does NOT import or reference SF Symbols for the main icon shapes
  </acceptance_criteria>
  <done>MenuBarIconView renders a composite clock+waterdrop icon with visually distinct states for idle, working, break, and paused</done>
</task>

<task type="auto">
  <name>Task 2: Wire MenuBarIconView into PomoHydroApp menu bar label</name>
  <files>PomoHydro/PomoHydro/PomoHydroApp.swift</files>
  <read_first>
    - PomoHydro/PomoHydro/PomoHydroApp.swift (current MenuBarExtra label)
    - PomoHydro/PomoHydro/Views/MenuBarIconView.swift (the new view from Task 1)
  </read_first>
  <action>
In PomoHydroApp.swift, replace the MenuBarExtra label closure (currently line 30):

**Before:**

```swift
} label: {
    Image(systemName: pomodoroService.menuBarIcon)
}
```

**After:**

```swift
} label: {
    MenuBarIconView(state: pomodoroService.state, allPaused: pomodoroService.allPaused)
}
```

This passes the live Pomodoro state and pause status to the custom icon. The `@Observable` PomodoroService will trigger view updates when state changes.

Note: `pomodoroService.allPaused` is an `@AppStorage` property — verify it's accessible. If not public, check if `@ObservationIgnored @AppStorage("allPaused")` is readable from outside the class (it should be since it's `var` not `private var`).

The `menuBarIcon` computed property on PomodoroService can remain for now — removing it is optional cleanup since nothing else references it after this change.
</action>
<verify>
<automated>grep -q "MenuBarIconView" PomoHydro/PomoHydro/PomoHydroApp.swift && ! grep -q 'Image(systemName: pomodoroService.menuBarIcon)' PomoHydro/PomoHydro/PomoHydroApp.swift && echo "PASS" || echo "FAIL"</automated>
</verify>
<acceptance_criteria> - PomoHydroApp.swift contains `MenuBarIconView(state:` in the label closure - PomoHydroApp.swift does NOT contain `Image(systemName: pomodoroService.menuBarIcon)` - No compile errors (Xcode build succeeds)
</acceptance_criteria>
<done>Menu bar renders the custom composite icon driven by live PomodoroService state</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify custom icon in menu bar</name>
  <files>PomoHydro/PomoHydro/Views/MenuBarIconView.swift</files>
  <action>Human verifies the custom composite icon renders correctly in the menu bar across all Pomodoro states.</action>
  <what-built>Custom composite clock+waterdrop menu bar icon that changes with Pomodoro state</what-built>
  <how-to-verify>
    1. Build and run PomoHydro in Xcode (Cmd+R)
    2. Check menu bar — you should see a composite clock+waterdrop icon instead of the old SF Symbol
    3. The icon should render correctly in your current appearance mode (light/dark)
    4. Click the icon to open the popover — start a Pomodoro session
    5. Verify the clock portion changes when state transitions: idle → working → break
    6. Use the pause toggle — verify the icon reflects the paused state
    7. Check the icon looks good at menu bar size (~18pt height)
  </how-to-verify>
  <verify>Human approves icon appearance</verify>
  <done>User confirms the custom menu bar icon looks correct and reflects all states</done>
  <resume-signal>Type "approved" or describe issues with the icon design</resume-signal>
</task>

</tasks>

<verification>
- Custom MenuBarIconView.swift exists with clock+waterdrop drawing
- PomoHydroApp.swift uses MenuBarIconView instead of SF Symbol
- Icon visually differentiates all Pomodoro states
- Waterdrop is always visible
- Icon renders correctly in menu bar at native size
</verification>

<success_criteria>

- Menu bar shows a unique composite clock+waterdrop icon (not an SF Symbol)
- All four Pomodoro states are visually distinguishable in the clock portion
- Waterdrop is constant and always visible
- Icon respects system appearance (light/dark mode)
- User approves the visual design
  </success_criteria>

<output>
After completion, create `.planning/phases/999.4-waterdrop-menubar-icon/999.4-01-SUMMARY.md`
</output>
